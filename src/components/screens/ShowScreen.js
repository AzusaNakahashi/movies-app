import React from "react"
import VideoContainer from "../containers/VideoContainer"


const ShowScreen = ({ navigation, route }) => <VideoContainer navigation={navigation} route={route}/>

export default ShowScreen